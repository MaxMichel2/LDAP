<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- BaseTheme defines what's common for all APIs -->
    <style name="Base.AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- Do not modify IssuerTheme -->
    <style name="BootstrapTheme" parent="Base.AppTheme" />

    <style name="BootstrapTheme.SplashScreenTheme" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/black</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash</item>
        <item name="windowSplashScreenAnimationDuration">600</item>
        <item name="postSplashScreenTheme">@style/BootstrapTheme</item>
    </style>
</resources>